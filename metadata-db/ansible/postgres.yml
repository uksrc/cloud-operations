---
- name: Install and configure PostgreSQL for SKA Metadata Manager
  hosts: all
  become: yes
  vars_files:
    - group_vars/all/variables.yml

  roles:
    - update_system
    - install_postgresql
    - configure_database
    - setup_metadata_schema
