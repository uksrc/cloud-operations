[defaults]
inventory = inventory
host_key_checking = False
retry_files_enabled = False
roles_path = roles
envfile = .env  # If Ansible is run from TF, this line has no effect, so TF must source the .env ahead of the run


[ssh_connection]
pipelining = True
