# Environment variables file containing secrets
.env

# Ansible retry files (already disabled in ansible.cfg)
*.retry

# User configuration file (use users.yml.example as template)
group_vars/all/users.yml

# Allowed IPs file (use xrootd_allowed_ips.yml.example as template)
group_vars/all/xrootd_allowed_ips.yml
